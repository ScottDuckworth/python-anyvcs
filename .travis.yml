language: python
python:
  - "2.7"
install: pip install .
before_script:
  - git --version
  - hg --version --quiet
  - svn --version --quiet
script:
  - export TEST_LOG_FILE=`mktemp`; python tests.py || (cat $TEST_LOG_FILE; false)
